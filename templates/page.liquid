{% if page.handle == 'data-sale-opt-out' %}
  <style>
    main {
      padding: 110px var(--side-padding) 40px var(--side-padding);
    }
  </style>
{% endif %}
<h1>{{ page.title }}</h1>
{{ page.content }}